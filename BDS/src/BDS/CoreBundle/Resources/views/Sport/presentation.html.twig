{%  extends "BDSCoreBundle:Sport:layout.html.twig" %}

{% block title %}
	{{ domaine.nom }} presentation - {{ parent() }}
{% endblock %}

{% block bds_sport_body %}
<div class="panel panel-default">
	<div class="panel-heading">
		<h3 class="panel-title">presentation de {{ domaine.nom }}</h3>
	</div>
	<div class="panel-body">
		<div class="col-lg-12 embed-responsive embed-responsive-16by9"">
			{% if domaine.urlVideo %}
					<iframe class="embed-responsive-item" src="{{ domaine.urlVideo }}" allowfullscreen></iframe>
			{% endif %}
		</div>
		{% if domaine.presentation %}
			<p>{{ domaine.presentation |  raw}}</p>
			<p><b>{{ domaine.capitaine.username }}</b></p>
		{% else %}
			<p> pas encore de présentation pour {{ domaine.nom }} mais cela sera fait rapidement !!</p>
		{% endif %}
	</div>
</div>

{# à sécuriser #}
<a class="btn btn-block btn-default" href="{{ path('bds_sport_presentation_edit', {'nom': domaine.nom}) }}">editer la présentation</a>
{% endblock %}